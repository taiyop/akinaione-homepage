doctype html
html
  head
    meta[charset="utf-8"]
    title
      | GMaps.js — Basic
    script[type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.6.4/jquery.min.js"]
    script[type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"]
    script[type="text/javascript" src="../gmaps.js"]
    link[rel="stylesheet" href="http://twitter.github.com/bootstrap/1.3.0/bootstrap.min.css"]
    link[rel="stylesheet" type="text/css" href="examples.css"]
    script[type="text/javascript"]
      |  var map; $(document).ready(function(){ map = new GMaps({ el: '#map', lat: -12.043333, lng: -77.028333, zoomControl : true, zoomControlOpt: { style : 'SMALL', position: 'TOP_LEFT' }, panControl : false, streetViewControl : false, mapTypeControl: false, overviewMapControl: false }); }); 
  body
    h1
      | GMaps.js — Basic
    .row
      .span11
        #map
      .span6
        p
          | Using GMaps.js is as easy as:
        pre
          | new GMaps({ el: '#map', lat: -12.043333, lng: -77.028333 });
        p
          | You must define 
          strong
            | container ID
          | , 
          strong
            | latitude
          |  and 
          strong
            | longitude
          |  of the map's center.
        p
          span.label.notice
            | Note: 
          | You also can define 
          strong
            | zoom
          | , 
          strong
            | width
          |  and 
          strong
            | height
          | . By default, zoom is 15. Width and height in a CSS class will replace these values.
