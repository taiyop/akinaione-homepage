doctype html
html
  head
    meta[charset="utf-8"]
    title
      | GMaps.js — Static map
    script[src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"]
    script[src="http://maps.google.com/maps/api/js?sensor=true"]
    script[src="../gmaps.js"]
    link[rel="stylesheet" href="http://twitter.github.com/bootstrap/1.3.0/bootstrap.min.css"]
    link[rel="stylesheet" type="text/css" href="examples.css"]
    script
      |  $(document).ready(function(){ var url = GMaps.staticMapURL({ size: [610, 350], lat: -12.043333, lng: -77.028333, styles: [{ featureType: "all", elementType: "all", stylers: [ { hue: '#FF0000' } ] }, { featureType: "landscape", elementType: "all", stylers: [ { hue: '#0A2D3F'}, { saturation: 50 }, { lightness: -10 } ] }] }); $('<img/>').attr('src', url).appendTo('#map'); }); 
  body
    h1
      | GMaps.js — Static map
    .row
      .span11
        #map
      .span6
        p
          | You can use the same styles as for a normal styled map to make a static map using GMaps.js:
        pre
          | url = GMaps.staticMapURL({ size: [610, 350], lat: -12.043333, lng: -77.028333 styles: [{ featureType: "all", elementType: "all", stylers: [ { hue: '#FF0000' } ] }, { featureType: "landscape", elementType: "all", stylers: [ { hue: '#0A2D3F'}, { saturation: 50 }, { lightness: -10 } ] }] }); $('<img/>').attr('src', url) .appendTo('#map');
